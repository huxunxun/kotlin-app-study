---
description: Kotlin + Jetpack Compose 编码规范与广告/桥接层最佳实践
globs: ["**/*.kt"]
alwaysApply: false
---

# Kotlin + Jetpack Compose 编码规范

## 代码风格

- 函数名：camelCase；类名：PascalCase；常量：UPPER_SNAKE_CASE
- Compose 组件函数名以 PascalCase 命名（如 `HomeScreen`、`AdBannerView`）
- 单表达式函数使用 `= ` 而非 `{ return }`
- 尽量使用 `val`，避免 `var`
- 使用 `data class` 表示纯数据；`object` 表示单例

## Compose UI

- 使用 `Modifier` 作为 Composable 函数的第一个可选参数
- 状态管理：`remember`、`mutableStateOf`、`derivedStateOf`
- 副作用：`LaunchedEffect`、`DisposableEffect`、`SideEffect`
- 列表性能：`LazyColumn` + `key`
- 内嵌原生 View 使用 `AndroidView`

## 广告模块使用

```kotlin
// 初始化（App.kt 中已自动完成）
AdManager.init(context)

// Banner（Compose 中直接使用）
AdBannerView()

// 插屏
AdManager.showInterstitial(activity)

// 激励视频
AdManager.showRewarded(activity, onRewarded = { /* 发奖励 */ })

// 扩展新广告商：实现 AdProvider 接口
class MyAdProvider : AdProvider { ... }
AdManager.setProvider(MyAdProvider())
```

## 桥接层（游戏引擎调用）

```kotlin
// 游戏引擎 → 原生
NativeBridge.callNative("showRewarded", "", "onRewardCallback")

// 原生 → 游戏引擎
NativeBridge.callGame("onRewardCallback", """{"rewarded":true}""")
```

## 网络层

```kotlin
// 创建 API 接口
val configApi = ApiClient.create(ConfigApi::class.java)

// 在协程中调用
val response = configApi.getAppConfig("app_id", "1.0")
if (response.isSuccess) {
    val config = response.data
}
```

## 架构约定

- 按功能模块组织代码：`ads/`、`bridge/`、`network/`、`ui/`、`util/`
- ViewModel 使用 `lifecycle-viewmodel-compose`
- 网络请求在 ViewModel 或 Repository 层的协程中发起
- 广告相关逻辑不直接写在 Composable 中，通过回调传递给 Activity
